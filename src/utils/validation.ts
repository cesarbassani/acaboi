// src/utils/validation.ts
import * as yup from 'yup';

export const abateSchema = yup.object({
  // esquema de validação para abates
});

export const produtorSchema = yup.object({
  // esquema de validação para produtores
});

// E assim por diante